{
  "name": "guitar-practice-app",
  "private": true,
  "scripts": {
    "build": "esbuild app/javascript/application.js --bundle --sourcemap --outdir=app/assets/builds --target=es2017"
  },
  "dependencies": {
    "esbuild": "^0.25.5"
  }
}
